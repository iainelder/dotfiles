# Minimal prompt.
PS1="$ "

# This makes it possible to copy and paste multiline commands to rerun them.
# The default prompt include a ">" which at best will break the command and
# at worst would make bash unexpectedly write a file.
PS2="  "
