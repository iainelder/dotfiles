name: AWS CodeBuild agent
on:
  push:
    paths:
      - programs/codebuild-agent/install.bash
      - .github/workflows/AWS CodeBuild agent.yml

  workflow_dispatch:

jobs:
  test_idempotent_installation:

    strategy:
      fail-fast: false
      matrix:
        os:
          - "ubuntu:20.04"
          - "ubuntu:22.04"

    runs-on: ubuntu-latest
    container:
      image: "${{ matrix.os }}"

    steps:

      - name: Check out code
        uses: actions/checkout@v4

      - name: Prepare Docker
        run: ./scripts/prepare_docker.bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install AWS CodeBuild agent
        shell: su norm --command "bash --login {0}"
        run: programs/codebuild-agent/install.bash

      - name: Install AWS CodeBuild agent again (test idempotency)
        shell: su norm --command "bash --login {0}"
        run: programs/codebuild-agent/install.bash
