name: s5cmd
on:
  push:
    paths:
      - programs/s5cmd/install.bash
      - .github/workflows/s5cmd.yml

  workflow_dispatch:

jobs:
  test_idempotent_installation:

    strategy:
      matrix:
        os:
          - "ubuntu:20.04"
          - "ubuntu:22.04"

    runs-on: ubuntu-latest
    container:
      image: "${{ matrix.os }}"

    steps:

      - name: Check out code
        uses: actions/checkout@v3

      - name: Prepare Docker
        run: ./scripts/prepare_docker.bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install s5cmd
        shell: su norm --command "bash --login {0}"
        run: programs/s5cmd/install.bash

      - name: Install s5cmd again (test idempotency)
        shell: su norm --command "bash --login {0}"
        run: programs/s5cmd/install.bash
