name: q
on:
  push:
    paths:
      - programs/q/install.bash
      - .github/workflows/q.yml

  workflow_dispatch:

jobs:
  test_idempotent_installation:

    strategy:
      matrix:
        os:
          - "ubuntu:20.04"
          - "ubuntu:22.04"

    runs-on: ubuntu-latest
    container:
      image: "${{ matrix.os }}"

    steps:

      - name: Check out code
        uses: actions/checkout@v3

      - name: Prepare Docker
        run: ./scripts/prepare_docker.bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install q
        shell: su norm --command "bash --login {0}"
        run: programs/q/install.bash

      - name: Install q again (test idempotency)
        shell: su norm --command "bash --login {0}"
        run: programs/q/install.bash
