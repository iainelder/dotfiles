name: Rocketchat
on:
  push:
    paths:
      - programs/rocketchat/install.bash
      - .github/workflows/Rocketchat.yml

  workflow_dispatch:

jobs:
  test_idempotent_installation:

    strategy:
      fail-fast: false
      matrix:
        os:
          - "ubuntu:20.04"
          - "ubuntu:22.04"

    runs-on: ubuntu-latest
    container:
      image: "${{ matrix.os }}"

    steps:

      - name: Check out code
        uses: actions/checkout@v4

      - name: Prepare Docker
        run: ./scripts/prepare_docker.bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Rocketchat
        shell: su norm --command "bash --login {0}"
        run: programs/rocketchat/install.bash

      - name: Install Rocketchat again (test idempotency)
        shell: su norm --command "bash --login {0}"
        run: programs/rocketchat/install.bash
