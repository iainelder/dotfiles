name: Firefox
on:
  push:
    paths:
      - programs/firefox/install.bash
      - .github/workflows/Firefox.yml

  workflow_dispatch:

jobs:
  test_idempotent_installation:

    strategy:
      fail-fast: false
      matrix:
        os:
          - "ubuntu:20.04"
          - "ubuntu:22.04"

    runs-on: ubuntu-latest
    container:
      image: "${{ matrix.os }}"

    steps:

      - name: Check out code
        uses: actions/checkout@v4

      - name: Prepare Docker
        run: ./scripts/prepare_docker.bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Firefox
        shell: su norm --command "bash --login {0}"
        run: programs/firefox/install.bash

      - name: Install Firefox again (test idempotency)
        shell: su norm --command "bash --login {0}"
        run: programs/firefox/install.bash
