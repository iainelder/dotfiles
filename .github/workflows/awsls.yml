name: awsls
on:
  push:
    paths:
      - programs/awsls/install.bash
      - .github/workflows/awsls.yml

  workflow_dispatch:

jobs:
  test_idempotent_installation:

    strategy:
      fail-fast: false
      matrix:
        os:
          - "ubuntu:20.04"
          - "ubuntu:22.04"

    runs-on: ubuntu-latest
    container:
      image: "${{ matrix.os }}"

    steps:

      - name: Check out code
        uses: actions/checkout@v4

      - name: Prepare Docker
        run: ./scripts/prepare_docker.bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install awsls
        shell: su norm --command "bash --login {0}"
        run: programs/awsls/install.bash

      - name: Install awsls again (test idempotency)
        shell: su norm --command "bash --login {0}"
        run: programs/awsls/install.bash
