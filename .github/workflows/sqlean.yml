name: SQLean
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  test_idempotent_installation:

    strategy:
      matrix:
        os:
          - "ubuntu:20.04"
          - "ubuntu:22.04"

    runs-on: ubuntu-latest
    container:
      image: "${{ matrix.os }}"

    steps:

      - name: Check out code
        uses: actions/checkout@v2

      - name: Prepare Docker
        run: ./scripts/prepare_docker.bash

      - name: Install SQLean
        shell: su norm --command "bash --login {0}"
        run: programs/sqlean/install.bash

      - name: Install SQLean again (test idempotency)
        shell: su norm --command "bash --login {0}"
        run: programs/sqlean/install.bash
