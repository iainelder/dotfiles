name: DVC (Data Version Control)
on:
  push:
    paths:
      - programs/dvc/install.bash

  workflow_dispatch:

jobs:
  test_idempotent_installation:

    strategy:
      matrix:
        os:
          - "ubuntu:20.04"
          - "ubuntu:22.04"

    runs-on: ubuntu-latest
    container:
      image: "${{ matrix.os }}"

    steps:

      - name: Check out code
        uses: actions/checkout@v3

      - name: Prepare Docker
        run: ./scripts/prepare_docker.bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install DVC (Data Version Control)
        shell: su norm --command "bash --login {0}"
        run: programs/dvc/install.bash

      - name: Install DVC (Data Version Control) again (test idempotency)
        shell: su norm --command "bash --login {0}"
        run: programs/dvc/install.bash
