name: firefox-container
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  test_idempotent_installation:

    strategy:
      matrix:
        os:
          - "ubuntu:20.04"
          - "ubuntu:22.04"

    runs-on: ubuntu-latest
    container:
      image: "${{ matrix.os }}"

    steps:

      - name: Check out code
        uses: actions/checkout@v2

      - name: Prepare Docker
        run: ./scripts/prepare_docker.bash

      - name: Install firefox-container
        shell: su norm --command "bash --login {0}"
        run: programs/firefox-open-url-in-container/install.bash

      - name: Install firefox-container again (test idempotency)
        shell: su norm --command "bash --login {0}"
        run: programs/firefox-open-url-in-container/install.bash
